// odd or even reporter

// write a for loop to iterate from 0 to 20

  // check if the current is even or odd

  // report the result to the screen
 var x = 0 ;
 for (x= 0; x<= 20; x++){
  if( x % 2 === 0){
 console.log(`the number ${x} is even` + x)
  } 
  else{
    console.log(`the number ${x} is odd` + x)
  }}
 
do{
  if( x % 2 === 0){
    console.log(`the number ${x} is evenkhaas` + x)
     } 
     else{
       console.log(`the number ${x} is oddkhas` + x)
     }
    x++;
}while(x <= 20);

while( x <= 20){
  
  if( x % 2 === 0){
    console.log(`the number ${x} is evenkha` + x)
     } 
     else{
       console.log(`the number ${x} is oddkh` + x)
     }
    
  x++; 
}
